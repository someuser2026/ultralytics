# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Global configuration YAML with settings and hyperparameters for YOLO training, validation, prediction and export
# For documentation see https://docs.ultralytics.com/usage/cfg/

task: detect # (str) YOLO task, i.e. detect, segment, classify, pose, obb
mode: train # (str) YOLO mode, i.e. train, val, predict, export, track, benchmark
angle_mode: oc # (str) OBB angle convention, e.g. oc or le90

# Train settings -------------------------------------------------------------------------------------------------------
model: # (str, optional) path to model file, i.e. yolov8n.pt or yolov8n.yaml
data: # (str, optional) path to data file, i.e. coco8.yaml
epochs: 100 # (int) number of epochs to train for
time: # (float, optional) max hours to train; training finishes the current epoch, then stops at the first limit reached
patience: 100 # (int) early stop after N epochs without val improvement
batch: 16 # (int) batch size; use -1 for AutoBatch
imgsz: 640 # (int | list) train/val use int (square); predict/export may use [h,w]
save: True # (bool) save train checkpoints and predict results
save_period: -1 # (int) save checkpoint every N epochs; disabled if < 1
cache: False # (bool | str) cache images in RAM (True/'ram') or on 'disk' to speed dataloading; False disables
device: # (int | str | list) device: 0 or [0,1,2,3] for CUDA, 'cpu'/'mps', or -1/[-1,-1] to auto-select idle GPUs
workers: 4 # (int) dataloader workers (per RANK if DDP)
project: # (str, optional) project name for results root
name: # (str, optional) experiment name; results in 'project/name'
exist_ok: False # (bool) overwrite existing 'project/name' if True
pretrained: True # (bool | str) use pretrained weights (bool) or load weights from path (str)
optimizer: auto # (str) optimizer: SGD, Adam, Adamax, AdamW, NAdam, RAdam, RMSProp, or auto
verbose: True # (bool) print verbose logs during training/val
seed: 0 # (int) random seed for reproducibility
deterministic: True # (bool) enable deterministic ops; reproducible but may be slower
single_cls: False # (bool) treat all classes as a single class
rect: False # (bool) rectangular batches for train; rectangular batching for val when mode='val'
cos_lr: False # (bool) cosine learning rate scheduler
close_mosaic: 10 # (int) disable mosaic augmentation for final N epochs (0 to keep enabled)
resume: False # (bool) resume training from last checkpoint in the run dir
amp: False # (bool) Automatic Mixed Precision (AMP) training; True runs AMP capability check
fraction: 1.0 # (float) fraction of training dataset to use (1.0 = all)
profile: False # (bool) profile ONNX/TensorRT speeds during training for loggers
freeze: # (int | list, optional) freeze first N layers (int) or specific layer indices (list)
unfreeze: # (int | list, optional) timm only: unfreeze last N inner blocks/stages (int) or specific inner indices (list) after the full timm backbone is frozen
lora: False # (bool) timm only: inject LoRA adapters into matching linear layers inside the wrapped timm backbone
lora_rank: 8 # (int) LoRA rank for timm-backed adapters
lora_alpha: 16 # (float) LoRA scaling alpha
lora_dropout: 0.0 # (float) dropout applied to the LoRA input path
lora_layers: # (int | list, optional) timm only: apply LoRA to the last N inner blocks/stages or to specific inner indices; default is all matching linear layers
lora_targets: # (list[str] | str) timm only: exact leaf names or dotted suffixes for linear modules to adapt
  - attn.qkv
  - attn.proj
multi_scale: False # (bool) multiscale training by varying image size
compile: False # (bool | str) enable torch.compile() backend='inductor'; True="default", False=off, or "default|reduce-overhead|max-autotune-no-cudagraphs"
reset_args: True # (bool) reset args when loading a PyTorch model checkpoint
reset_args_keys:
  - imgsz
  - data
  - task
  - single_cls

# Segmentation
overlap_mask: true # (bool) merge instance masks into one mask during training (segment only)
mask_ratio: 8 # (int) mask downsample ratio (segment only)
pointrend: false # (bool) enable the model-agnostic PointRend segmentation refiner
pointrend_mode: joint # (str) PointRend training mode: joint or frozen
pointrend_feature_levels: [0] # (list[int]) segmentation-head feature levels used for fine point features
pointrend_project_channels: 256 # (int) projected channels per selected fine feature level
pointrend_hidden_channels: 256 # (int) hidden channels in the PointRend point MLP
pointrend_num_fcs: 3 # (int) number of hidden point-MLP layers
pointrend_coarse_resolution: 28 # (int) square ROI resolution used as the coarse PointRend mask
pointrend_train_num_points: 196 # (int) uncertainty-biased points supervised per training instance
pointrend_oversample_ratio: 3.0 # (float) random candidate oversampling ratio for training points
pointrend_importance_sample_ratio: 0.75 # (float) fraction of training points chosen by uncertainty
pointrend_train_max_instances: 100 # (int) maximum PointRend training instances per image
pointrend_subdivision_steps: 3 # (int) iterative 2x refinement steps at inference
pointrend_subdivision_num_points: 784 # (int) uncertain pixels replaced per inference subdivision
pointrend_scale_factor: 2 # (int) mask upsampling factor per subdivision
pointrend_loss_weight: 1.0 # (float) PointRend point-loss gain
use_shoreline_aux_loss: false # (bool) force shoreline Gaussian auxiliary targets for compatible shore-aux heads (useful when training from checkpoints)
use_shoreline_prior_loss: false # (bool) penalize confident OBB or segment predictions far from shoreline
use_land_water_prior_loss: false # (bool) penalize confident OBB or segment predictions that place too much support over land/sand
shoreline_aux_weight: 0.20 # (float) target loss gain for the shoreline Gaussian auxiliary head
active_shoreline_aux_weight: 0.0 # (float) placeholder only do not use. active weight during warmpup for aux heads
shoreline_aux_warmup_epochs: 10 # (int) epochs used to linearly ramp the shoreline auxiliary loss from 0 to its target gain
shoreline_aux_gaussian_sigma_ratio: 0.035 # (float) Gaussian sigma as a fraction of the max image dimension for shoreline proximity targets
shoreline_aux_gaussian_truncate_sigmas: 3.0 # (float) truncate the shoreline Gaussian target outside this many sigmas
shoreline_aux_bce_weight: 1.0 # (float) BCE-with-logits term weight for the shoreline auxiliary loss
shoreline_aux_dice_weight: 1.0 # (float) soft Dice term weight for the shoreline auxiliary loss
shoreline_prior_point_mode: center # (str) point sampled for shoreline prior: center or closest_corner
shoreline_prior_weight: 1.0 # (float) shoreline prior loss gain
land_water_prior_weight: 1.0 # (float) land/water prior loss gain
shoreline_prior_max_dist: 128 # (int) shoreline distance cap in pixels for normalization
shoreline_prior_gt_margin: 0.05 # (float) normalized distance margin before segment GT-relative shoreline prior activates
segment_prior_topk: 512 # (int|float) segment prior unassigned anchors per image; -1 or inf evaluates all anchors
land_water_prior_land_threshold: 0.05 # (float) fraction of valid prediction support allowed over land/sand before land-water prior activates
land_water_prior_exp_beta: 4.0 # (float) exponential sharpness for land-water prior above threshold
dual_input_backbone: false # (bool) route RGB and auxiliary channels through parallel backbones when the model YAML supports it
dual_input_rgb_channels: 3 # (int) number of leading channels reserved for the RGB backbone branch
dual_input_fusion: concat_proj # (str) feature fusion style for dual-input backbones
seg_use_mixed_loss: true # (bool) use mixed loss for segmentation
seg_w_lovasz: 0.0 # (float) weight for lovasz loss for segmentation
seg_w_dice: 0.0 # (float) weight for dice loss for segmentation
seg_w_bce: 1 # (float) weight for bce loss for segmentation
seg_ignore_index: -100 # (int) ignore index for segmentation
seg_area_normalize: False # (bool) False: no normalization, True: normalize by image area
use_soft_ignore_band: false
ignore_band_width: 10
soft_ignore_transition_ratio: 0.5
use_ultrafast_ignore_band: true


# Classification
dropout: 0.0 # (float) dropout for classification head (classify only)

# Val/Test settings ----------------------------------------------------------------------------------------------------
val: True # (bool) run validation/testing during training
split: val # (str) dataset split to evaluate: 'val', 'test' or 'train'
save_json: False # (bool) save results to COCO JSON for external evaluation
conf: # (float, optional) confidence threshold; defaults: predict=0.25, val=0.001
iou: 0.7 # (float) IoU threshold used for NMS
max_det: 300 # (int) maximum number of detections per image
half: False # (bool) use half precision (FP16) if supported
dnn: False # (bool) use OpenCV DNN for ONNX inference
plots: True # (bool) save plots and images during train/val
# comparision_metric: fitness # (str) metric to use for comparison, choices=[fitness, f1, f2, custom]

fitness_weights:
  precision: 0.10      # (float) weight for precision
  recall: 0.30         # (float) weight for recall  
  mAP50: 0.30          # (float) weight for mAP@0.5
  mAP50_95: 0.20       # (float) weight for mAP@0.5:0.95
  mask_precision: 0.0  # (float) weight for mask precision
  mask_recall: 0.0     # (float) weight for mask recall
  mask_mAP50: 0.05     # (float) weight for mask mAP@0.5
  mask_mAP50_95: 0.0   # (float) weight for mask mAP@0.5:0.95
  f1: 0.0              # (float) weight for F1 score
  f2: 0.05             # (float) weight for F2 score
  dice: 0.0            # (float) weight for Dice coefficient
  miou: 0.0            # (float) weight for mean IoU
  boundary_f1: 0.0     # (float) weight for boundary F1 score

# Additional segmentation metric settings ------------------------------------------------------------------------------
boundary_tolerance: 2 # (int) pixel tolerance for boundary-F1 (segmentation metrics)

# Predict settings -----------------------------------------------------------------------------------------------------
source: # (str, optional) path/dir/URL/stream for images or videos; e.g. 'ultralytics/assets' or '0' for webcam
vid_stride: 1 # (int) read every Nth frame for video sources
stream_buffer: False # (bool) True buffers all frames; False keeps the most recent frame for low-latency streams
visualize: False # (bool) visualize model features (predict) or TP/FP/FN confusion (val)
augment: False # (bool) apply test-time augmentation during prediction
agnostic_nms: False # (bool) class-agnostic NMS
classes: # (int | list[int], optional) filter by class id(s), e.g. 0 or [0,2,3]
retina_masks: False # (bool) use high-resolution segmentation masks (segment)
embed: # (list[int], optional) return feature embeddings from given layer indices

# Visualize settings ---------------------------------------------------------------------------------------------------
show: False # (bool) show images/videos in a window if supported
save_frames: False # (bool) save individual frames from video predictions
save_txt: False # (bool) save results as .txt files (xywh format)
save_conf: False # (bool) save confidence scores with results
save_crop: False # (bool) save cropped prediction regions to files
show_labels: True # (bool) draw class labels on images, e.g. 'person'
show_conf: True # (bool) draw confidence values on images, e.g. '0.99'
show_boxes: True # (bool) draw bounding boxes on images
line_width: # (int, optional) line width of boxes; auto-scales with image size if not set

# Export settings ------------------------------------------------------------------------------------------------------
format: torchscript # (str) target format, e.g. torchscript|onnx|openvino|engine|coreml|saved_model|pb|tflite|edgetpu|tfjs|paddle|mnn|ncnn|imx|rknn
keras: False # (bool) TF SavedModel only (format=saved_model); enable Keras layers during export
optimize: False # (bool) TorchScript only; apply mobile optimizations to the scripted model
int8: False # (bool) INT8/PTQ where supported (openvino, tflite, tfjs, engine, imx); needs calibration data/fraction
dynamic: False # (bool) dynamic shapes for torchscript, onnx, openvino, engine; enable variable image sizes
simplify: True # (bool) ONNX/engine only; run graph simplifier for cleaner ONNX before runtime conversion
opset: # (int, optional) ONNX/engine only; opset version for export; leave unset to use a tested default
workspace: # (float, optional) engine (TensorRT) only; workspace size in GiB, e.g. 4
nms: False # (bool) fuse NMS into exported model when backend supports; if True, conf/iou apply (agnostic_nms except coreml)

# Hyperparameters ------------------------------------------------------------------------------------------------------
lr0: 0.01 # (float) initial learning rate (SGD=1e-2, Adam/AdamW=1e-3)
lrf: 0.01 # (float) final LR fraction; final LR = lr0 * lrf
momentum: 0.937 # (float) SGD momentum or Adam beta1
weight_decay: 0.0005 # (float) weight decay (L2 regularization)
warmup_epochs: 3.0 # (float) warmup epochs (fractions allowed)
warmup_momentum: 0.8 # (float) initial momentum during warmup
warmup_bias_lr: 0.1 # (float) bias learning rate during warmup
box: 7.5 # (float) box loss gain
mask_weight: 5.0 # (float) mask loss gain
cls: 0.5 # (float) classification loss gain
dfl: 1.5 # (float) distribution focal loss gain
pose: 12.0 # (float) pose loss gain (pose tasks)
kobj: 1.0 # (float) keypoint objectness loss gain (pose tasks)
nbs: 64 # (int) nominal batch size used for loss normalization
hsv_h: 0.015 # (float) HSV hue augmentation fraction
hsv_s: 0.7 # (float) HSV saturation augmentation fraction
hsv_v: 0.4 # (float) HSV value (brightness) augmentation fraction
degrees: 0.0 # (float) rotation degrees (+/-)
translate: 0.1 # (float) translation fraction (+/-)
scale: 0.5 # (float) scale gain (+/-)
shear: 0.0 # (float) shear degrees (+/-)
perspective: 0.0 # (float) perspective fraction (0–0.001 typical)
flipud: 0.0 # (float) vertical flip probability
fliplr: 0.5 # (float) horizontal flip probability
bgr: 0.0 # (float) RGB↔BGR channel swap probability
mosaic: 1.0 # (float) mosaic augmentation probability
mixup: 0.0 # (float) MixUp augmentation probability
cutmix: 0.0 # (float) CutMix augmentation probability
copy_paste: 0.0 # (float) segmentation copy-paste probability
copy_paste_mode: flip # (str) copy-paste strategy for segmentation: flip or mixup
auto_augment: randaugment # (str) classification auto augmentation policy: randaugment, autoaugment, augmix
erasing: 0.4 # (float) random erasing probability for classification (0–0.9), <1.0
clahe_p: 0.0
clahe_clip_limit: 2.5
clahe_tile_grid_size: [8, 8]
gamma_min: 0.6
gamma_max: 0.9
rand_gamma_p: 0.0
unsharp_blur_limit_min: 3
unsharp_blur_limit_max: 5
unsharp_sigma_limit: 0.6
unsharp_alpha_min: 0.6
unsharp_alpha_max: 0.8
unsharp_p: 0.0
unsharp_threshold: 10
edgeboost_alpha: 0.25
edgeboost_ksize: 3
edgeboost_p: 0.0
homomorphic_gain: 0.5
homomorphic_sigma: 12
homomorphic_p: 0.0
gaussian_blur_p: 0.0
motion_blur_p: 0.0
additive_noise_p: 0.0
multi_spec_noise_p: 0.0
multi_ch_albu: True



# Custom config.yaml ---------------------------------------------------------------------------------------------------
cfg: # (str, optional) path to a config.yaml that overrides defaults

# Tracker settings ------------------------------------------------------------------------------------------------------
tracker: botsort.yaml # (str) tracker config file: botsort.yaml or bytetrack.yaml
