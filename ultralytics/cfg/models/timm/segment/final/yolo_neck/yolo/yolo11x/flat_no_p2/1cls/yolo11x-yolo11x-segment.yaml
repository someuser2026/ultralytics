# Ultralytics 🚀 AGPL-3.0 License - https://ultralytics.com/license

# Ultralytics YOLO11-seg instance segmentation model with P3/8 - P5/32 outputs
# Model docs: https://docs.ultralytics.com/models/yolo11
# Task docs: https://docs.ultralytics.com/tasks/segment

# Parameters
nc: 1 # number of classes
stride: 224
scales: # model compound scaling constants, i.e. 'model=yolo11n-obb.yaml' will call yolo11-obb.yaml with scale 'n'
  # [depth, width, max_channels]
  # n: [0.50, 0.25, 1024] # summary: 196 layers, 2695747 parameters, 2695731 gradients, 6.9 GFLOPs
  # s: [0.50, 0.50, 1024] # summary: 196 layers, 9744931 parameters, 9744915 gradients, 22.7 GFLOPs
  # m: [0.50, 1.00, 512] # summary: 246 layers, 20963523 parameters, 20963507 gradients, 72.2 GFLOPs
  # l: [1.00, 1.00, 512] # summary: 372 layers, 26220995 parameters, 26220979 gradients, 91.3 GFLOPs
  x: [1.00, 1.50, 512] # summary: 372 layers, 58875331 parameters, 58875315 gradients, 204.3 GFLOPs

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2 (96, 320, 320)
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4 (192, 160, 160)
  - [-1, 2, C3k2, [256, False, 0.25]] # (384, 160, 160)
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8 # (384, 80, 80)
  - [-1, 2, C3k2, [512, False, 0.25]] # (768, 80, 80)
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16 (768, 40, 40)
  - [-1, 2, C3k2, [512, True]] # (768, 40, 40)
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32 (768, 20, 20)
  - [-1, 2, C3k2, [1024, True]] # (768, 20, 20)
  - [-1, 1, SPPF, [1024, 5]] # 9 (768, 20, 20)
  - [-1, 2, C2PSA, [1024]] # 10 (768, 20, 20)

# YOLO11n head
head:
  # --------------------------------------------------------------------------
  # Step 2: Apply PANet Neck (replaces your manual TD+BU paths)
  # --------------------------------------------------------------------------
  # Original config has:
  # - Top-down: P5→P4→P3→P2 with concat fusion + C2f
  # - Bottom-up: P2→P3→P4→P5 with concat fusion + C2f
  # - Output channels: [192, 384, 768, 768] at scales [x4, x8, x16, x32]
  
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # (768, 40, 40)
  - [[-1, 6], 1, Concat, [1]] # cat backbone P4 (N, 40, 40)
  - [-1, 2, C3k2, [512, False]] # 13 (768, 40, 40)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # (768, 80, 80)
  - [[-1, 4], 1, Concat, [1]] # cat backbone P3 (N, 80, 80)
  - [-1, 2, C3k2, [256, False]] # 16 (P3/8-small) (384, 80, 80)

  - [-1, 1, Conv, [256, 3, 2]] # (384, 40, 40)
  - [[-1, 13], 1, Concat, [1]] # cat head P4 (N, 40, 40)
  - [-1, 2, C3k2, [512, False]] # 19 (P4/16-medium) (768, 40, 40)

  - [-1, 1, Conv, [512, 3, 2]] # (768, 20, 20)
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2, [1024, True]] # 22 (P5/32-large)

  # --------------------------------------------------------------------------
  # Step 4: Segmentation Head (using 3 scales)
  # --------------------------------------------------------------------------
  - [[16, 19, 22], 1, Segment, [nc, 32, 256]]
